<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body, class: "form-control newart" %>
  </p>
  <p>
    <%= f.submit class: "btn btn-primary newart"  %>
  </p>
<% end %>
